/**
 * Push notification registration hook.
 * Currently disabled — notifications infrastructure is in place but
 * expo-notifications is not installed. Re-enable by installing the package
 * and uncommenting the logic below.
 */

export function useNotifications() {
  // No-op: push notifications disabled for now.
  // When ready, install expo-notifications + expo-device,
  // add "expo-notifications" to app.json plugins,
  // and restore the registration logic from notifications.ts.
}
